model:
  name: "LaMa"
  weights_path: "models/inpainting/weights/lama.pth"

runtime:
  device: "cuda"        # "cuda" or "cpu"

inference:
  # fallback_opencv: true - REMOVED
  # opencv_method: "telea"   # REMOVED
  batch_size: 1
