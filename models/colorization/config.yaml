model:
  name: "DeOldify"
  weights_path: "models/colorization/weights/colorizer.pth"

runtime:
  device: "cuda"     # "cuda" or "cpu"

inference:
  # fallback_opencv: true - REMOVED
  # saturation_boost: 1.2 - REMOVED
  batch_size: 1
